import { registerRootComponent } from 'expo';
import App from '../../App'; // Adjust this path based on your directory structure

// Register the main app component so the app can load from App.js
registerRootComponent(App);
